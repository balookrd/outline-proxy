// TODO(task-6): `payload`'s CRUD types (`CreateBody`, `UpdateBody`, `DeleteBody`,
// `ReorderBody`, `RouteListEntry`, `RoutesListResponse`, `MutationResponse`) and
// helpers (`payload_to_table`, `table_is_default`, `route_revision`,
// `route_table_to_json`) aren't consumed yet — the dispatcher lands in Task 6.
// Remove this once that commit wires them up.
#![allow(dead_code)]

mod payload;
