# Template — NOT applied by deploy.sh (*.example.yaml is skipped).
# Create the real secret out of band, keep the password out of git:
#
#   kubectl -n monitoring create secret generic grafana-admin \
#     --from-literal=password='<STRONG_PASSWORD>'
#
# Or edit a copy of this file and `kubectl apply -f` it yourself.
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: monitoring
type: Opaque
stringData:
  password: "CHANGE_ME"
