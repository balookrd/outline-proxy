# Server Mesh Cluster (anycast ingress + shard relay to home)

*Русская версия: [CLUSTER.ru.md](CLUSTER.ru.md)*

How several `outline-ss-rust` servers can act as one cluster so that an
`outline-ws-rust` client may enter through **any** server, while a session
always stays pinned to its **home** server. A non-home server (the **edge**)
that the client happens to reach forwards the still-encrypted application
bytes to the home server over a QUIC **mesh** link, routed by a shard id
embedded in the session id. If the home is unreachable or the mesh is too
slow, the system degrades gracefully to a fresh session — exactly the
behaviour you get today when servers know nothing about each other.

This document only covers VLESS and combined-SS carriers; split-SS is out of
scope for the cluster.

## Why "migrate the session to another server" is not literal

The parked state on a server is a *live* upstream connection: an established
TCP socket to the target with its kernel sequence numbers, source address and
connection state (`ParkedTcp`), or a bound `UdpSocket`
(`ParkedVlessUdpSingle`). None of that can be serialized and resurrected on a
different machine. So the session is never moved. Instead it **stays on the
home server**, and the carrier (the client-facing WebSocket / HTTP3 / QUIC
transport) is what becomes anycast: it may land on any edge, which relays the
bytes back to home. From the client's point of view this looks like "park on
server A, unpark on server B"; physically the state lived on home the whole
time.

## Roles

- **home** — the server that owns a session: the upstream connection to the
  target, the SS/VLESS crypto state, and the resumption parking
  (`OrphanRegistry`). Chosen once, at first connect, and pinned for the life
  of the session.
- **edge** — the server that terminated the client carrier. May *be* the home
  (everything is local) or be a different server (it relays to home).
- **mesh** — the authenticated QUIC link edge → home that carries the
  still-encrypted application bytes.
- **shard id** — a 4-bit home identifier embedded in the session id.

## Shard id in the session id

A resumption `SessionId` is 16 bytes, generated by the home on the first
handshake and returned in `X-Outline-Session` (HTTP/WS) or VLESS addon `0x10`.
Today it is pure CSPRNG with no structure. The cluster reserves the home
identity inside it:

```
SessionId = obfuscate_k( shard_id (4 bits) || nonce (124 bits) )
            └─ keyed PRF/CTR under the shared cluster routing key,
               so the id still looks fully random on the wire
```

- **4 bits → up to 16 servers.** 124 bits of nonce remain — far more than
  enough against resume-id guessing.
- **obfuscation** turns the structured value into a wire-random one. The shard
  is a *routing hint, not a secret*, but its structure must not be visible to
  a DPI/fingerprint observer, so it is encrypted under a cluster-wide
  `cluster_routing_key`. Any cluster member de-obfuscates and reads the shard
  in O(1); there is no shared routing table for ids.
- Encode/decode live in `outline-wire` (a new `cluster` module) so the server
  binary and the mesh path use one implementation.

The combined-SS `SsPathKind` (TCP vs UDP) bit lives in the XHTTP URL / WS
`/{token}` segment, **not** in the 16-byte resume id, so it does not contend
for these bits.

## Routing decision on the edge

The edge must know the shard **before** any SS/VLESS payload. The shard is
available in carrier metadata that arrives ahead of the body:

| Carrier | Where the shard is | When visible |
| --- | --- | --- |
| WS / H3 / XHTTP resume | `X-Outline-Resume: <hex>` header | at upgrade, before body |
| VLESS raw QUIC resume | addon `0x11 RESUME_ID` (16 bytes) | in the first VLESS frame |
| First connect (no resume) | — | edge *is* home: handle locally, mint a session id with `shard = self` |

```
            client (ws-rust)
          resume? X-Outline-Resume / addon 0x11
                      │ carrier (WS/H3/QUIC)
                      ▼
                  ┌────────┐
                  │  EDGE  │  decode_shard(session_id)
                  └───┬────┘
        shard==self   │   shard!=self
        ┌─────────────┘└──────────────┐
        ▼                              ▼
  LOCAL (as today)            MESH RELAY to home[shard]
  accept→crypto→park          terminate carrier, stream
  →upstream→target            app bytes over mesh QUIC
                                       │
                                       ▼
                              HOME[shard]
                              accept / crypto / park / unpark
                              upstream → target   (state lives only here)
```

**First connect picks home.** A client with no resume id lands on some edge,
which handles it locally and returns a session id carrying its own shard. All
later resumes from that client — through any edge — point back to that home.
So the home is chosen where the client first established the session, normally
the nearest working server.

## Mesh transport (relay boundary = raw application bytes)

The edge terminates **only the carrier** (WS frames / H3 stream / QUIC),
extracts the application bytes (the still-encrypted SS/VLESS stream as-is) and
tunnels them to home. **Crypto and the upstream connection live only on home.**
The edge never holds keys and never sees plaintext.

- **Link:** long-lived **QUIC** connections between cluster members,
  authenticated with mTLS (aws-lc-rs — the single crypto provider already in
  the graph; quinn is already a dependency). One QUIC connection per peer
  pair, multiplexing many sessions.
- **One QUIC stream per session.** This is the key reason QUIC was chosen over
  a hand-rolled framing on top of mTLS-TCP: per-stream flow control gives
  honest backpressure for free and removes head-of-line blocking between
  sessions — the exact failure class that bit the TUN pump. No blind
  coalescing.

Mesh stream control messages:

```
OPEN  { session_id, carrier_kind, client_meta (resume headers / addons), peer_addr_hint }
DATA  { dir: up | down, bytes }          // application bytes, both directions
CLOSE { reason: fin | abort | budget }   // graceful FIN / RST / health-budget hit
```

For VLESS mux, the whole multiplex parks atomically, so the relay carries the
entire multiplex over one mesh stream and never splits sub-connections across
streams.

### Health budget ("slow neighbour → tear down")

Because cluster members live in different countries, the edge → home hop is
always long. The mesh is therefore best-effort, not a reliable backbone:

- The edge measures *progress* per session: `no_progress_ms` (in-flight bytes
  with no DATA coming back from home) and the mesh handshake RTT.
- Exceeding `mesh_relay_budget_ms` makes the edge close the client carrier
  with an ordinary resume-miss/close, so the client starts a **fresh session
  locally on the edge** (the edge becomes the new home; the orphaned park on
  the slow home expires by TTL).
- The budget is about *progress*, not absolute RTT — high RTT between
  countries is expected and tolerated; a stalled relay is not.

## Home: park/unpark unchanged

Because the relay boundary is the still-encrypted byte stream, a relayed
session is **indistinguishable** to the home from a direct client carrier.
The `OrphanRegistry`, the `Parked` variants and the resume wire protocol work
**without changes**. The only difference is that the "client transport" is a
mesh stream from an edge rather than a socket from the client.

"Park on A, unpark on B" then falls out for free: the client leaves edge A (or
edge A dies), home parks as it would on any carrier drop; the client arrives at
edge B with the same session id, edge B opens a fresh mesh stream to home, and
home unparks into that stream.

## Failure semantics

| Failure | Behaviour | Session |
| --- | --- | --- |
| edge dies | client fails over to another edge (existing uplink logic); same session id → same home → unpark | **survives** |
| mesh too slow | edge tears down on health budget → fresh session on the edge | lost (acceptable) |
| home dies | shard points at a dead server; edge cannot reach it over mesh → resume-miss → fresh session | lost (= today) |
| shard unknown (server decommissioned) | edge treats it as home-down → fresh session | lost |

## Security

- **Mesh auth:** mTLS between cluster members (shared CA / pinned certs); an
  outsider cannot inject a relay stream.
- **Topology is not revealed:** on home-down or foreign-owner the edge answers
  with an ordinary resume-miss (as the existing owner-mismatch path stays
  silent). An observer cannot distinguish "no such shard" from "home is down".
- **Shard is obfuscated** under the cluster key, so the session id stays
  wire-random for DPI.
- **The edge never sees plaintext** (boundary = encrypted application bytes),
  so compromising an edge does not expose other sessions' traffic — only
  metadata (a session id; the target is inside the SS/VLESS stream the edge
  does not parse).

## Configuration

Server (`outline-ss-rust`):

```toml
[cluster]
enabled = true
shard_id = 7                       # 0..15, unique within the cluster
cluster_routing_key = "<base64>"   # shared key obfuscating the shard in the session id
mesh_listen = "[::]:9443"          # mTLS QUIC listener for inbound relay
mesh_ca = "..."; mesh_cert = "..."; mesh_key = "..."
mesh_relay_budget_ms = 4000        # progress budget: stalled longer → tear down
peers = [
  { shard = 1, addr = "..." },
  { shard = 7, addr = "self" },
  # ...
]
```

Client (`outline-ws-rust`): **ideally no changes.** The cluster is transparent
to the client — it already resumes to the same uplink with the same session
id, and the edge failover path already exists. Anycast ingress and the relay
are invisible to it.

## Bounded resources

Per the workspace "bounded resources" invariant, every long-lived mesh object
has an explicit limit:

- mesh streams: a cap on active relays per peer and globally, with LRU
  eviction.
- a relayed session inherits the home parking TTL; the edge also keeps its own
  idle timeout.
- the health budget is the upper bound on a hanging relay.

## Open risks

1. **Mesh data plane is not covered by unit tests** (same as the TUN pump) —
   needs e2e coverage plus sha256 verification of large transfers on real
   traffic; keep `git revert` ready.
2. **Double hop across countries** raises RTT for long bulk transfers; the
   health budget protects against hangs but not against slowness. A possible
   follow-up: let the client dial the home directly when it is reachable,
   bypassing the edge relay — but that is client-side routing on top of the
   mesh, a separate phase.
3. **VLESS mux** parks atomically; the relay must carry the whole multiplex on
   one mesh stream, never splitting it, or partial-resume breaks.
